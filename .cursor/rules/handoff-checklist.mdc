---
description: Before any handoff — run code review, tech debt evaluation, tests/coverage, and optional project readiness; record in handoff note
globs: "**/.cursor/handoff/*.md,AGENT_HANDOFF.md"
alwaysApply: false
---

# Handoff checklist (mandatory before handoff)

When the user wants a handoff, or when you are writing or updating a handoff note (e.g. in `.cursor/handoff/`) or AGENT_HANDOFF "Current state":

**You must run all four and record results in the handoff note. Do not skip any.**

1. **Code review** — Use the **code-reviewer** skill (`.cursor/skills/code-reviewer/SKILL.md`). Review the changed or relevant code since last handoff (or main branch). Output PASS/WARN/FAIL and a short summary. Put the summary in the handoff note under **Code review**.
2. **Tech debt evaluation** — Use the **tech-debt-evaluator** skill (`.cursor/skills/tech-debt-evaluator/SKILL.md`). Assess current debt; produce prioritized list. Put the short summary or "Do first" items in the handoff note under **Tech debt**.
3. **Tests / coverage** — Run the test and coverage commands documented in **AGENT_HANDOFF.md** (and TEST_PLAN.md). Note whether the suite is green and, if useful, a one-line summary. Put in the handoff note under **Code coverage** (or **Tests**).
4. **Project readiness (optional)** — If your project has a readiness skill (e.g. game-readiness, product-readiness), use it and put the summary under **Project readiness**. Otherwise note "N/A" or skip that section.

5. **Tier 2 (integration / E2E) if needed** — If you added or changed behavior that requires integration or E2E validation, run the Tier 2 command from TEST_PLAN.md. Note in the handoff whether Tier 2 was run and the result. See `.cursor/rules/testing.mdc` and TEST_PLAN.md.

6. **Git commit** — If the first four checks passed: stage the changes and run `git commit` with a short message. If any check failed or is blocking, report back and do not commit until fixed.

Then complete the rest of the handoff (Current state, Done this session, Next up, etc.).
