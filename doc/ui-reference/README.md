# UI reference (third-party inspiration)

PNG screenshots in `auto-refresh-plus/` are **not** URL Auto Refresher branding. They document a commercial extension (**Auto Refresh Plus**–style) used only as **layout and workflow inspiration** for agents and implementers.

**Canonical scope and checkboxes:** [EDGE_URL_AUTO_REFRESHER_PLAN.md](../plan/EDGE_URL_AUTO_REFRESHER_PLAN.md) — section **Reference UI + agent guidance (checklist)**.

## Expected files (`auto-refresh-plus/`)

| File | Contents |
|------|----------|
| `time-interval-tab.png` | Interval / presets / custom time UI (reference only). |
| `active-tabs-list.png` | List of tabs with URLs and per-row countdown. |
| `page-monitor-tab.png` | Text find/lose monitoring (maps loosely to Epic 9). |

Add these files when you have exports; until then **Ref.1** in the EDGE plan stays open.
